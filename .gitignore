# Generated / disposable — never committed
workspace/*
data/*
!data/.gitkeep
playground/*
!playground/.gitkeep
!playground/README.md

# Legacy Obsidian vault — retired from the hub's memory story; ignore any
# machine-local leftover so it never lands in git.
wiki/

# The one project-specific file — never committed.
config/hub.config.yaml

# Env shim — machine-local (a token could be pasted in one day).
.claude/hub-env

# Python / misc
__pycache__/
*.pyc
.pytest_cache/
.venv/
.DS_Store
